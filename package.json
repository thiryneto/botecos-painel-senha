{
  "name": "botecos-senha",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "set NODE_ENV=development&& ts-node-dev --inspect --transpile-only --ignore-watch node-modules src/server.ts",
    "deploy": "ts-node-dev --inspect --transpile-only --ignore-watch node-modules src/server.ts",
    "prod": "set NODE_ENV=production&& ts-node-dev --inspect --transpile-only --ignore-watch node-modules src/server.ts",
    "clean": "rm -rf dist",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "node-windows": "^1.0.0-beta.8",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/socket.io": "^3.0.2",
    "ts-node-dev": "^2.0.0",
    "tsup": "^6.7.0",
    "tsx": "^3.12.7",
    "typescript": "^5.0.4"
  }
}
